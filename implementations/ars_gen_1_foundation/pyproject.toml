[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "ars_recursive"
version = "0.1.0"
description = "ARS Recursive Implementation (Generation 9)"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "ars_recursive._arslib"
python-source = "."
